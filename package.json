{
  "name": "ct-api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "better-auth-generate": "npx @better-auth/cli@latest generate --config ./better-auth.config.ts",
    "db:generate": "prisma generate --no-engine"
  },
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "better-auth": "^1.3.3",
    "hono": "^4.8.8"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "prisma": "^6.12.0",
    "wrangler": "^4.4.0"
  }
}
